import {sha256Canonical} from "../core/digest.js";import type {PageSnapshot} from "./adapters.js";export function createSnapshot(url:string,title:string,text:string,capturedAt=new Date().toISOString()):PageSnapshot{return{url,title,text,capturedAt,digest:sha256Canonical({url,title,text})}}export function verifySnapshot(x:PageSnapshot){return x.digest===sha256Canonical({url:x.url,title:x.title,text:x.text})}