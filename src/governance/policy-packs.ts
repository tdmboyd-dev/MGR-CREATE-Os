import {sha256Canonical} from "../core/digest.js";export interface PolicyRule<T=unknown>{id:string;effect:"ALLOW"|"DENY"|"REQUIRE_APPROVAL";when(input:T):boolean;reason:string}export interface PolicyPack<T=unknown>{id:string;version:string;rules:PolicyRule<T>[]}export function evaluatePolicyPack<T>(p:PolicyPack<T>,input:T){const matched=p.rules.filter(r=>r.when(input));const outcome=matched.some(r=>r.effect==="DENY")?"DENY":matched.some(r=>r.effect==="REQUIRE_APPROVAL")?"REQUIRE_APPROVAL":"ALLOW";return{packId:p.id,version:p.version,outcome,reasons:matched.map(r=>r.reason),inputDigest:sha256Canonical(input)}}