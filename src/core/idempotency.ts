export type IdempotencyStatus="STARTED"|"SUCCEEDED"|"FAILED";export interface IdempotencyRecord<T=unknown>{key:string;requestDigest:string;status:IdempotencyStatus;result?:T;error?:string}export class IdempotencyStore{private m=new Map<string,IdempotencyRecord>();begin(key:string,requestDigest:string){const p=this.m.get(key);if(p){if(p.requestDigest!==requestDigest)throw Error("idempotency key reused with different request");return p}const x={key,requestDigest,status:"STARTED" as const};this.m.set(key,x);return x}succeed<T>(key:string,result:T){const x=this.must(key) as IdempotencyRecord<T>;x.status="SUCCEEDED";x.result=structuredClone(result);return x}fail(key:string,error:string){const x=this.must(key);x.status="FAILED";x.error=error;return x}get(key:string){return this.m.get(key)}private must(key:string){const x=this.m.get(key);if(!x)throw Error("idempotency record missing");return x}}