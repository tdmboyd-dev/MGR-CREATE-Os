export interface SecretGrant{id:string;credentialRef:string;tenantId:string;operatorId:string;scopes:string[];tokenHandle:string;issuedAt:number;expiresAt:number;revokedAt?:number}export class SecretBroker{private g=new Map<string,SecretGrant>();issue(x:SecretGrant){if(x.expiresAt<=x.issuedAt)throw new Error("invalid grant lifetime");this.g.set(x.id,Object.freeze({...x}));return x}assertUsable(id:string,tenantId:string,operatorId:string,scope:string,now=Date.now()){const x=this.g.get(id);if(!x)throw new Error("grant not found");if(x.tenantId!==tenantId||x.operatorId!==operatorId)throw new Error("grant scope mismatch");if(x.revokedAt||now>=x.expiresAt)throw new Error("grant expired/revoked");if(!x.scopes.includes(scope))throw new Error("grant missing scope");return x}}