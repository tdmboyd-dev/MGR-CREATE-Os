export type Reversibility="REVERSIBLE"|"COMPENSATABLE"|"IRREVERSIBLE";export interface CompensationStep{id:string;reversibility:Reversibility;compensate?:()=>Promise<void>}export async function compensate(steps:CompensationStep[]){const results=[];for(const s of [...steps].reverse()){if(s.reversibility==="IRREVERSIBLE"){results.push({id:s.id,status:"IRREVERSIBLE"});continue}if(!s.compensate){results.push({id:s.id,status:"NO_HANDLER"});continue}try{await s.compensate();results.push({id:s.id,status:"COMPENSATED"})}catch(e){results.push({id:s.id,status:"FAILED",error:String(e)});break}}return results}