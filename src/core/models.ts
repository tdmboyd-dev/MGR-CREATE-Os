export interface EvalSnapshot{benchmark:string;version:string;quality:number;latencyMs:number;costPerUnit:number;at:string}export interface ModelVersion{id:string;provider:string;revision:string;capabilities:string[];license:string;privacy:"LOCAL"|"PRIVATE_API"|"PUBLIC_API";healthy:boolean;evals:EvalSnapshot[]}export interface RouteRequest{capability:string;allowedLicenses?:string[];maxCostPerUnit?:number;privacy?:ModelVersion["privacy"]}export class ModelRouter{constructor(private models:ModelVersion[]){ }route(r:RouteRequest){const c=this.models.filter(m=>m.healthy&&m.capabilities.includes(r.capability)&&(!r.allowedLicenses||r.allowedLicenses.includes(m.license))&&(!r.privacy||m.privacy===r.privacy)).filter(m=>r.maxCostPerUnit===undefined||Math.min(...m.evals.map(e=>e.costPerUnit))<=r.maxCostPerUnit).sort((a,b)=>Math.max(...b.evals.map(e=>e.quality))-Math.max(...a.evals.map(e=>e.quality)));if(!c[0])throw new Error("no compliant model");return c[0]}}