export interface Dependency{id:string;version:string;license:string;requires:{id:string;range:string}[]}export interface Resolution{ordered:Dependency[];licenseConflicts:string[];missing:string[]}export function resolveDependencies(root:string,all:Dependency[],allowedLicenses:string[]):Resolution{const by=new Map(all.map(x=>[x.id,x])),seen=new Set<string>(),ordered:Dependency[]=[],missing:string[]=[],licenseConflicts:string[]=[];function visit(id:string){if(seen.has(id))return;seen.add(id);const x=by.get(id);if(!x){missing.push(id);return}for(const d of x.requires)visit(d.id);if(!allowedLicenses.includes(x.license))licenseConflicts.push(`${x.id}:${x.license}`);ordered.push(x)}visit(root);return{ordered,missing,licenseConflicts}}