export interface EvalCase<I=unknown,E=unknown>{id:string;input:I;expected:E}export interface EvalSuite<I=unknown,E=unknown>{id:string;version:string;capability:string;cases:EvalCase<I,E>[];threshold:number}export interface EvalResult{id:string;suiteId:string;suiteVersion:string;subjectRef:string;score:number;passed:boolean;caseResults:{caseId:string;score:number;evidence:string}[];at:string}export async function runEval<I,E>(suite:EvalSuite<I,E>,subjectRef:string,score:(c:EvalCase<I,E>)=>Promise<{score:number;evidence:string}>){const cr=[];for(const c of suite.cases)cr.push({caseId:c.id,...await score(c)});const total=cr.length?cr.reduce((s,x)=>s+x.score,0)/cr.length:0;return{id:`eval:${suite.id}:${subjectRef}`,suiteId:suite.id,suiteVersion:suite.version,subjectRef,score:total,passed:total>=suite.threshold,caseResults:cr,at:new Date().toISOString()} satisfies EvalResult}