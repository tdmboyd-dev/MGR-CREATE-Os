export interface DeletionTarget{system:string;resourceId:string;delete():Promise<void>;verifyDeleted():Promise<boolean>}export async function executeDeletion(targets:DeletionTarget[]){const results=[];for(const t of targets){try{await t.delete();const gone=await t.verifyDeleted();results.push({system:t.system,resourceId:t.resourceId,status:gone?"DELETED":"VERIFY_FAILED"})}catch(e){results.push({system:t.system,resourceId:t.resourceId,status:"FAILED",error:String(e)})}}return{results,complete:results.every(r=>r.status==="DELETED")}}