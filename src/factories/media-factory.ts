import type {Factory,FactoryRequest,FactoryResult} from "./contracts.js";import {sha256Canonical} from "../core/digest.js";export interface MediaInput{modality:"IMAGE"|"VIDEO"|"AUDIO"|"3D";brief:string;modelCapability:string;lockRefs:string[];rightsRefs:string[];verificationCriteria:string[]}export class MediaFactory implements Factory{kind="MEDIA" as const;async execute(req:FactoryRequest):Promise<FactoryResult>{const x=req.inputs as MediaInput;const b=[];if(!x.brief)b.push("missing media brief");if(!x.verificationCriteria.length)b.push("missing verification criteria");if(!x.rightsRefs.length)b.push("missing rights provenance");return{requestId:req.id,kind:this.kind,status:b.length?"BLOCKED":"SUCCEEDED",artifactRefs:b.length?[]:["media_"+sha256Canonical(x).slice(0,20)],evidenceRefs:[...x.lockRefs,...x.rightsRefs],blockers:b.length?b:undefined}}}