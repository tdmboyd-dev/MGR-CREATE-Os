import type {Factory,FactoryRequest,FactoryResult} from "./contracts.js";import {sha256Canonical} from "../core/digest.js";export interface CourseInput{objectives:string[];lessons:{id:string;objectiveIds:string[]}[];assessments:{id:string;objectiveIds:string[]}[];sourceRefs:string[]}export class CourseFactory implements Factory{kind="COURSE" as const;async execute(req:FactoryRequest):Promise<FactoryResult>{const x=req.inputs as CourseInput;const covered=new Set(x.assessments.flatMap(a=>a.objectiveIds)),b=x.objectives.filter(o=>!covered.has(o)).map(o=>"objective lacks assessment: "+o);return{requestId:req.id,kind:this.kind,status:b.length?"BLOCKED":"SUCCEEDED",artifactRefs:b.length?[]:["course_"+sha256Canonical(x).slice(0,20)],evidenceRefs:x.sourceRefs,blockers:b.length?b:undefined}}}