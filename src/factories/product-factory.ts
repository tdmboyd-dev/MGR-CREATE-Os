import type {Factory,FactoryRequest,FactoryResult} from "./contracts.js";import {sha256Canonical} from "../core/digest.js";export interface ProductInput{opportunityEvidenceRefs:string[];title:string;audience:string;problem:string;deliverables:string[];sourceRefs:string[]}export class ProductFactory implements Factory{kind="PRODUCT" as const;async execute(req:FactoryRequest):Promise<FactoryResult>{const x=req.inputs as ProductInput;const blockers:string[]=[];if(!x.opportunityEvidenceRefs.length)blockers.push("missing opportunity evidence");if(!x.audience)blockers.push("missing audience");if(!x.problem)blockers.push("missing buyer problem");if(!x.deliverables.length)blockers.push("missing deliverables");if(blockers.length)return{requestId:req.id,kind:this.kind,status:"BLOCKED",artifactRefs:[],evidenceRefs:x.sourceRefs,blockers};const id="product_"+sha256Canonical(x).slice(0,20);return{requestId:req.id,kind:this.kind,status:"SUCCEEDED",artifactRefs:[id],evidenceRefs:[...x.opportunityEvidenceRefs,...x.sourceRefs]}}}