export interface DistributionManifest{productRef:string;channels:{id:string;requiredAssets:string[];metadataFields:string[]}[];availableAssets:string[];metadata:Record<string,string>}export function validateDistribution(m:DistributionManifest){const missing:string[]=[];for(const c of m.channels)for(const a of c.requiredAssets)if(!m.availableAssets.includes(a))missing.push(c.id+":"+a);for(const c of m.channels)for(const f of c.metadataFields)if(!m.metadata[f])missing.push(c.id+":metadata:"+f);return{valid:!missing.length,missing}}