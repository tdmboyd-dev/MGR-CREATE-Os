import type {Factory,FactoryRequest,FactoryResult} from "./contracts.js";import {sha256Canonical} from "../core/digest.js";export interface DocumentInput{title:string;sections:{heading:string;body:string;sourceRefs?:string[]}[];formats:string[]}export class DocumentFactory implements Factory{kind="DOCUMENT" as const;async execute(req:FactoryRequest):Promise<FactoryResult>{const x=req.inputs as DocumentInput;if(!x.title||!x.sections.length)return{requestId:req.id,kind:this.kind,status:"BLOCKED",artifactRefs:[],evidenceRefs:[],blockers:["document missing title/sections"]};const id="doc_"+sha256Canonical(x).slice(0,20);return{requestId:req.id,kind:this.kind,status:"SUCCEEDED",artifactRefs:x.formats.map(f=>id+"."+f.toLowerCase()),evidenceRefs:x.sections.flatMap(s=>s.sourceRefs??[])}}}