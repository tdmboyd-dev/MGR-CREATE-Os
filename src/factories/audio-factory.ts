import type {Factory,FactoryRequest,FactoryResult} from "./contracts.js";import {sha256Canonical} from "../core/digest.js";export interface AudioInput{scriptRef:string;voiceConsentRef?:string;musicRightsRefs:string[];pronunciationTerms:string[];qcCriteria:string[]}export class AudioFactory implements Factory{kind="AUDIO" as const;async execute(req:FactoryRequest):Promise<FactoryResult>{const x=req.inputs as AudioInput,b=[];if(!x.scriptRef)b.push("missing script");if(!x.qcCriteria.length)b.push("missing audio QC");return{requestId:req.id,kind:this.kind,status:b.length?"BLOCKED":"SUCCEEDED",artifactRefs:b.length?[]:["audio_"+sha256Canonical(x).slice(0,20)],evidenceRefs:[...(x.voiceConsentRef?[x.voiceConsentRef]:[]),...x.musicRightsRefs],blockers:b.length?b:undefined}}}