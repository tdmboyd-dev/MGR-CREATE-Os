export interface Offer{productRef:string;audience:string;problem:string;promise:string;deliverables:string[];priceCandidates:number[];upsells:string[];evidenceRefs:string[]}export function validateOffer(o:Offer){const blockers=[];if(!o.audience)blockers.push("missing audience");if(!o.problem)blockers.push("missing problem");if(!o.promise)blockers.push("missing promise");if(!o.deliverables.length)blockers.push("missing deliverables");if(!o.evidenceRefs.length)blockers.push("missing evidence");if(o.priceCandidates.some(p=>p<0))blockers.push("invalid price");return{valid:!blockers.length,blockers}}