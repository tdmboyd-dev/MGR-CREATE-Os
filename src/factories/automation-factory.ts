import type {Factory,FactoryRequest,FactoryResult} from "./contracts.js";import {sha256Canonical} from "../core/digest.js";export interface AutomationInput{trigger:string;steps:{operator:string;sideEffect:boolean}[];rollbackDefined:boolean}export class AutomationFactory implements Factory{kind="AUTOMATION" as const;async execute(req:FactoryRequest):Promise<FactoryResult>{const x=req.inputs as AutomationInput;if(x.steps.some(s=>s.sideEffect)&&!x.rollbackDefined)return{requestId:req.id,kind:this.kind,status:"BLOCKED",artifactRefs:[],evidenceRefs:[],blockers:["side effects require rollback/compensation plan"]};return{requestId:req.id,kind:this.kind,status:"SUCCEEDED",artifactRefs:["automation_"+sha256Canonical(x).slice(0,20)],evidenceRefs:[]}}}