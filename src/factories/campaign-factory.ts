import type {Factory,FactoryRequest,FactoryResult} from "./contracts.js";import {sha256Canonical} from "../core/digest.js";export interface CampaignInput{audienceEvidenceRefs:string[];claimsEvidenceRefs:string[];channels:string[];trackingPlan:string[];spendAuthorized:boolean}export class CampaignFactory implements Factory{kind="CAMPAIGN" as const;async execute(req:FactoryRequest):Promise<FactoryResult>{const x=req.inputs as CampaignInput;const b=[];if(!x.audienceEvidenceRefs.length)b.push("missing audience evidence");if(!x.trackingPlan.length)b.push("missing tracking plan");return{requestId:req.id,kind:this.kind,status:b.length?"BLOCKED":"SUCCEEDED",artifactRefs:b.length?[]:["campaign_"+sha256Canonical({...x,spendAuthorized:false}).slice(0,20)],evidenceRefs:[...x.audienceEvidenceRefs,...x.claimsEvidenceRefs],blockers:b.length?b:undefined}}}