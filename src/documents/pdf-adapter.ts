import type {DocumentRenderer,RenderedDocument} from "./renderers.js";export interface PdfEngine{renderHtml(html:string):Promise<Uint8Array>}export class PdfRenderer implements DocumentRenderer{format="PDF";constructor(private engine:PdfEngine){}async render(x:{title:string;sections:{heading:string;body:string}[]}):Promise<RenderedDocument>{const esc=(s:string)=>s.replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;");const html=`<!doctype html><html><head><meta charset="utf-8"><title>${esc(x.title)}</title></head><body><h1>${esc(x.title)}</h1>${x.sections.map(s=>`<h2>${esc(s.heading)}</h2><p>${esc(s.body)}</p>`).join("")}</body></html>`;const bytes=await this.engine.renderHtml(html);if(bytes.length<5||new TextDecoder().decode(bytes.slice(0,5))!=="%PDF-")throw Error("PDF engine returned invalid PDF signature");return{format:this.format,bytes,mime:"application/pdf",extension:"pdf"}}}