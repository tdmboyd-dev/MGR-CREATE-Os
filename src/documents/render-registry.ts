import type {DocumentRenderer} from "./renderers.js";export class RendererRegistry{private m=new Map<string,DocumentRenderer>();register(r:DocumentRenderer){if(this.m.has(r.format))throw Error("renderer exists");this.m.set(r.format,r)}get(format:string){const r=this.m.get(format.toUpperCase());if(!r)throw Error("renderer missing: "+format);return r}formats(){return[...this.m.keys()]}}