export interface RenderedDocument{format:string;bytes:Uint8Array;mime:string;extension:string}export interface DocumentRenderer{format:string;render(input:{title:string;sections:{heading:string;body:string}[]}):Promise<RenderedDocument>}export class HtmlRenderer implements DocumentRenderer{format="HTML";async render(x:{title:string;sections:{heading:string;body:string}[]}){const esc=(s:string)=>s.replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;");const html=`<!doctype html><html><head><meta charset="utf-8"><title>${esc(x.title)}</title></head><body><h1>${esc(x.title)}</h1>${x.sections.map(s=>`<section><h2>${esc(s.heading)}</h2><p>${esc(s.body)}</p></section>`).join("")}</body></html>`;return{format:this.format,bytes:new TextEncoder().encode(html),mime:"text/html",extension:"html"}}}