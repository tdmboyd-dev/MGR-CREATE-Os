import type {DocumentRenderer,RenderedDocument} from "./renderers.js";export interface DocxEngine{render(x:{title:string;sections:{heading:string;body:string}[]}):Promise<Uint8Array>}export class DocxRenderer implements DocumentRenderer{format="DOCX";constructor(private engine:DocxEngine){}async render(x:{title:string;sections:{heading:string;body:string}[]}):Promise<RenderedDocument>{const bytes=await this.engine.render(x);if(bytes.length<4||bytes[0]!==0x50||bytes[1]!==0x4b)throw Error("DOCX engine returned non-ZIP payload");return{format:this.format,bytes,mime:"application/vnd.openxmlformats-officedocument.wordprocessingml.document",extension:"docx"}}}