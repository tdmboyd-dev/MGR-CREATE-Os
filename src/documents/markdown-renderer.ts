import type {DocumentRenderer} from "./renderers.js";export class MarkdownRenderer implements DocumentRenderer{format="MARKDOWN";async render(x:{title:string;sections:{heading:string;body:string}[]}){const text="# "+x.title+"\n\n"+x.sections.map(s=>"## "+s.heading+"\n\n"+s.body).join("\n\n");return{format:this.format,bytes:new TextEncoder().encode(text),mime:"text/markdown",extension:"md"}}}