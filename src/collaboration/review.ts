export type Decision="APPROVE"|"REJECT"|"CHANGES_REQUESTED";export interface Annotation{id:string;artifactVersionId:string;actorId:string;body:string;frame?:number;timeMs?:number;region?:{x:number;y:number;w:number;h:number};createdAt:string}export interface VersionDecision{id:string;artifactVersionId:string;actorId:string;decision:Decision;criteria:string[];evidenceRefs:string[];createdAt:string}export function assertDecisionApplies(d:VersionDecision,artifactVersionId:string){if(d.artifactVersionId!==artifactVersionId)throw new Error("decision belongs to different version");return true}