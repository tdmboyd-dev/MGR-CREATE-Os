import type {ActorContext} from "../core/tenancy.js";export interface CatalogRecord{id:string;tenantId:string;workspaceId:string;type:string;versionRef:string;title:string;tags:string[];text:string;updatedAt:string}export class Catalog{private r:CatalogRecord[]=[];put(x:CatalogRecord){this.r=this.r.filter(y=>y.id!==x.id);this.r.push({...x})}search(a:ActorContext,q:string){const s=q.toLowerCase();return this.r.filter(x=>x.tenantId===a.tenantId&&x.workspaceId===a.workspaceId&&(x.title.toLowerCase().includes(s)||x.text.toLowerCase().includes(s)||x.tags.some(t=>t.toLowerCase().includes(s))))}}