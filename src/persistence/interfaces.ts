import type {CreationRun,StageRun,EventEnvelope,EvidenceRecord} from "../core/types.js";import type {AssetVersion} from "../creation/assets.js";import type {UCTRecord} from "../creation/uct.js";export interface CreationRepository{saveRun(x:CreationRun):Promise<void>;getRun(id:string):Promise<CreationRun|undefined>;saveStage(x:StageRun):Promise<void>;stages(runId:string):Promise<StageRun[]>}export interface EventRepository{append(e:EventEnvelope):Promise<boolean>;markPublished(id:string):Promise<void>;pending():Promise<EventEnvelope[]>}export interface AssetRepository{saveVersion(v:AssetVersion):Promise<void>;getVersion(assetId:string,version:number):Promise<AssetVersion|undefined>}export interface ProvenanceRepository{saveUCT(u:UCTRecord):Promise<void>;getUCT(id:string):Promise<UCTRecord|undefined>;saveEvidence(tenantId:string,e:EvidenceRecord):Promise<void>}