export interface S3LikeClient{putObject(x:{Bucket:string;Key:string;Body:Uint8Array;ContentType?:string;Metadata?:Record<string,string>}):Promise<{ETag?:string}>;getObject(x:{Bucket:string;Key:string}):Promise<{Body:Uint8Array}>;deleteObject(x:{Bucket:string;Key:string}):Promise<void>;headObject(x:{Bucket:string;Key:string}):Promise<unknown>}export class S3CompatibleObjectStore{constructor(private client:S3LikeClient,private bucket:string){}async put(key:string,data:Uint8Array,metadata?:Record<string,string>){const r=await this.client.putObject({Bucket:this.bucket,Key:key,Body:data,Metadata:metadata});return{key,etag:r.ETag??""}}async get(key:string){return(await this.client.getObject({Bucket:this.bucket,Key:key})).Body}async delete(key:string){await this.client.deleteObject({Bucket:this.bucket,Key:key})}async exists(key:string){try{await this.client.headObject({Bucket:this.bucket,Key:key});return true}catch{return false}}}