import {sha256Canonical} from "../core/digest.js";export interface AuditEntry{id:string;tenantId:string;actorId:string;action:string;resourceRef?:string;at:string;previousDigest?:string;digest:string}export class AuditLog{private entries:AuditEntry[]=[];append(x:Omit<AuditEntry,"digest"|"previousDigest">){const previousDigest=this.entries.at(-1)?.digest;const base={...x,previousDigest};const e={...base,digest:sha256Canonical(base)};this.entries.push(Object.freeze(e));return e}verify(){let prev: string|undefined;for(const e of this.entries){const {digest,...base}=e;if(e.previousDigest!==prev||sha256Canonical(base)!==digest)return false;prev=digest}return true}list(){return [...this.entries]}}