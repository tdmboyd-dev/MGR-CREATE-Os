function privateHost(h:string){const x=h.toLowerCase();if(x==="localhost"||x==="::1"||x.endsWith(".local"))return true;if(/^127\./.test(x)||/^10\./.test(x)||/^192\.168\./.test(x))return true;const m=x.match(/^172\.(\d+)\./);if(m&&+m[1]>=16&&+m[1]<=31)return true;if(x==="169.254.169.254"||x.startsWith("169.254."))return true;return false}export function assertSafeExternalUrl(raw:string){const u=new URL(raw);if(!["http:","https:"].includes(u.protocol))throw Error("unsupported URL protocol");if(privateHost(u.hostname))throw Error("private/local network target denied");if(u.username||u.password)throw Error("URL credentials denied");return u}