const SECRET_KEYS=/password|secret|token|api[_-]?key|authorization|cookie/i;export function redact(value:unknown):unknown{if(Array.isArray(value))return value.map(redact);if(value&&typeof value==="object"){const out:Record<string,unknown>={};for(const [k,v] of Object.entries(value as Record<string,unknown>))out[k]=SECRET_KEYS.test(k)?"[REDACTED]":redact(v);return out}return value}