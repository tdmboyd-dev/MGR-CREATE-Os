import type {EventRepository} from "../persistence/interfaces.js";export class OutboxPublisher{constructor(private repo:EventRepository,private publish:(e:any)=>Promise<void>){}async flush(){const pending=await this.repo.pending();const results=[];for(const e of pending){try{await this.publish(e);await this.repo.markPublished(e.id);results.push({id:e.id,status:"PUBLISHED"})}catch(err){results.push({id:e.id,status:"FAILED",error:String(err)})}}return results}}