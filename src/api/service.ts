import {sha256Canonical} from "../core/digest.js";import {IdempotencyStore} from "../core/idempotency.js";import type {ObjectivePlan} from "../objectives/orchestrator.js";import {ObjectiveOrchestrator} from "../objectives/orchestrator.js";export class CreationOSService{private idem=new IdempotencyStore();constructor(private objectives:ObjectiveOrchestrator){}async executeObjective(key:string,plan:ObjectivePlan){const digest=sha256Canonical(plan),r=this.idem.begin(key,digest);if(r.status==="SUCCEEDED")return r.result;try{const result=await this.objectives.execute(plan);this.idem.succeed(key,result);return result}catch(e){this.idem.fail(key,String(e));throw e}}}