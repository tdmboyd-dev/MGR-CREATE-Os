# tool connectors — RESEARCHED

Separate Capability from Connector. Study MCP, n8n, Activepieces, OAuth connectors. Credentials are secret references; connectors declare scopes, tenant binding, read/write/side-effect class, idempotency, pagination, rate limits, health and reconciliation. Build Connector SDK + capability registry; MCP is one adapter. Verify expired/revoked OAuth and partial API success.

BEAST disposition: ADAPT. Implementation remains separate from research status.
