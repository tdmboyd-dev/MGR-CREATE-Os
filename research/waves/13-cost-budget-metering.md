# cost budget metering — RESEARCHED

Legacy estimated/actual cost is fragmented. Build append-only UsageEvent/CostLedger: provider/model/tool, quantity/unit, price snapshot, tenant/project/run/stage, free/included quota, actual billed if known. Forecast as range/confidence, not fake precision. Budget policy warns/throttles/approval/deny. Verify sampled reconciliation against provider usage.

BEAST disposition: ADAPT. Implementation remains separate from research status.
