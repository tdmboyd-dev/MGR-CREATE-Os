# interoperability mcp sdk api — RESEARCHED

Standalone OS needs canonical service/domain API first; SDK wraps it, events/webhooks notify, MCP exposes selected capabilities/resources. Current MCP 2026-07-28 uses stateless request/response core, cacheable deterministic discovery, hardened authorization and extensions/tasks. MCP is not the internal kernel. Verify version/auth/catalog/webhook/task/cancel contracts.

BEAST disposition: ADAPT. Implementation remains separate from research status.
