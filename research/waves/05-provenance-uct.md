# provenance uct — RESEARCHED

UCT schema exists; issuer/verifier unproven. Study OpenLineage + Sigstore/GitHub attestations. UCT should be immutable artifact record with digest, parent inputs, run/stage, model/tool/version, config refs, locks, approvals, evidence, rights, cost; optional signed attestation. Verify mutation breaks digest and full lineage reconstructs.

BEAST disposition: ADAPT unless implementation evaluation proves ADOPT or REJECT.
