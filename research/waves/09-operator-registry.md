# operator registry — RESEARCHED

Old 80-operator count is unverified. Operator becomes versioned capability: JSON input/output schema, side-effect class, permissions, cost, timeout/retry/idempotency, sandbox, provider adapters, verifier, deprecation. Counts derive from registry. MCP exposes safe subset. Contract-test every implementation.

BEAST disposition: ADAPT unless implementation evaluation proves ADOPT or REJECT.
